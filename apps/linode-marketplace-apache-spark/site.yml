---
# Spark Ansible playbook

- name: Configure Spark nodes
  hosts: all
  any_errors_fatal: true
  user: root
  vars:
    local_home: "{{ lookup('env','HOME') }}"
  vars_files: [group_vars/linode/vars, group_vars/linode/secret_vars]
  roles: [common, spark, post]
